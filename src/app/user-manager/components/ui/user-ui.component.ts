import {Component, Input, Output, EventEmitter, OnInit, SimpleChanges, OnChanges} from "@angular/core";

@Component({
    selector : "user-ui",
    template :``
})
export class UserUi
{
    constructor() {
    }
}
